# Copy to sandbox-local/.env (git-ignored) and fill in. deploy-sandbox.sh loads
# it as DEFAULTS (lib/env-defaults.sh): anything already exported wins.
#
# Mainnet RPC used to fork anvil. Leave empty to deploy with mock price feeds.
MAINNET_RPC_URL=
